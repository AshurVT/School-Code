#include <stdio.h>
#include <stdlib.h>

	int main(int argc, char *argv[]) {
		int start = atoi(argv[1]);
		int n = start;
		int fib(n)

		{
			if (n <= 1)
				return n;
			return fib(n-1)+fib(n-2);}
	 
		{
			int n = start;
			
		printf("the %dth intiger of the fibonacci sequence is %d\n", start,  fib(n));
		return 0;
		}}

